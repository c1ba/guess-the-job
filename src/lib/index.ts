// place files you want to import through the `$lib` alias in this folder.
import {startTimer, timer} from './stores/timer';
import {result, setResult} from './stores/result';
import dataStore from './stores/data';
export {startTimer, timer, result, setResult, dataStore};
